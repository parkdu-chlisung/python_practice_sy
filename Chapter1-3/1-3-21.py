# 문제 21 : set은 어떻게 만드나요?
# 다음 중 set을 만드는 방법이 아닌것?

# 1) x = {1,2,3,5,6,7} -> 가능
# set은 중복을 허락하지 않음
# a = {1,2,3}
# b = {3,4,5}
# 합집합 a | b
# 교집합 a & b
# 차집합 a - b , b - a
# 2) x = {} -> 불가능
# 이건 dict
# 3) x = set('python') -> 가능
# 순서가 지켜지지는 않는다.
# set은 순서가 없기 때문이다.
# 4) x = set(range(5)) -> 가능
# 5) x = set() -> 가능

# **
